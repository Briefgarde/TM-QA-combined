# TODO this file take a model and loads into memory such that the model can now be used to take inputs and generate output,  as well as potentially be fine tuned. The file itself doesn't do the fine tuning.  
# load_model.py just returns (model, tokenizer) given a name/path. 
# It's possible each model will have its particular file, to avoid crowding things. 